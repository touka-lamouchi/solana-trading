import { logger } from "../utils/logger";

// Stub — will connect to Twitter, Telegram, Reddit APIs in Phase 8
export class OffchainFeeds {
  async start(): Promise<void> {
    logger.info("Off-chain feeds: stub ready (connects in Phase 8)");
  }
}